#include <iostream>
#include <vector>
using namespace std;

class Solution{
public:
    bool BruteForce(vector<vector<int>> &v, int key){
        bool result = 0;
        for (int i = 0; i < v.size(); i++){
            for (int j = 0; j < v[i].size(); j++){
                if 
            }
        }
        return result;
    }
};

int main(){
    vector<vector<int>> v= {{1,2,3,50},{4,5,6,24},{7,8,9,10}};
    Solution s;
    int key = 3;
    s.BruteForce(v, key);
    return 0;
}
